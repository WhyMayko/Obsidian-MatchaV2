# Icons

The root files are the nine runtime icons preloaded by `Loader.lua`.

`lucide/` contains the complete white 48px PNG catalog generated from the official Lucide icons at https://lucide.dev/icons/.

The catalog is stored for future UI use and is not preloaded by the runtime.
